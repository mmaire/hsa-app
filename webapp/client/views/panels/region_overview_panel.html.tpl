% setdefault("id", "region-overview-panel");
% setdefault("hide", False);
<div
   id="{{id}}"
   class="region-overview-panel panel-right{{" hidden" if hide else ""}}">
   <div id="region-overview-label" class="text-medium">
      Region Overview
   </div>
   <p>Type:
   <p>Label:
   <p>Stats: (size, #subregions, etc)
</div>
